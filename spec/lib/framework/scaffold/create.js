var tap = require('tap');

var frameworkScaffoldCreate = require('./../../../../lib/framework/scaffold/create');

tap.test(function(t) {
    // t.plan(1);
    // frameworkScaffoldCreate({
        // componentName: 'foobar',
        // username: 'matthew',
        // email: 'matthew@famo.us'
    // }, function() {
        // t.ok(1);
    // });
});
