#!/usr/bin/env node

var program = require('commander');

var create = require('../lib/user/create');

program
  .command('create')
  .description('create a user for famous hub')
  .action(create);

program
  .parse(process.argv);

